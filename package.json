{
  "name": "cowlsly-chat",
  "version": "0.1.0",
  "private": true,
  "description": "Cowlsly.cc responsive messenger demo shell",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
